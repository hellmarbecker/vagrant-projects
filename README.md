# vagrant-projects

Collection of scripts and files to fire up a Hadoop cluster from a single Vagrant command. First version uses shell scripts for provisioning.

Later, I may change this to use Puppet.
